# 当前交接摘要（2026-09-12，闭环 03）

工程：`C:\Users\Lenovo\Desktop\ALLCODE\Uni-app`，原生 HarmonyOS ArkTS / ArkUI。
分支：`main`。今日按用户授权组织为三个本地提交，用户自行推送。

| 提交 | 闭环 |
|---|---|
| `1b71dfa` | Mock 店铺浏览、空店/重试、选物页视觉 |
| `58cef16` | 商品详情、SKU、数量、加购与购物车实际数据展示 |
| 本记录所在提交：`feat: 完成购物车交互与首页商品入口闭环` | 购物车操作、预估合计、角标、首页搜索与导航 |

## 现在已经实现

- 首页 → 店铺目录 / 商品详情 / 购物车 / 我的。首页采用已认可的编辑式视觉，删除了只弹“待接入”的促销、地址等首页入口。
- 本地商品/店铺搜索：250ms 防抖，支持清空、无结果、失败重试；请求序号防止旧结果覆盖新输入；切底栏保留关键词。
- 商品详情 → SKU / 数量 → 加购 → 购物车。详情展示主图、价格库存、店铺、商品说明与规格信息。未虚构运费、配送时效、销量、评价或服务承诺。
- 购物车使用真实 Mock 状态：SKU 合并、跨店分组、单项/店铺/全选、独立数量增减、单项/已选批量移除（确认后执行）。
- 勾选、详情、数量、移除为独立操作热区；使用原生圆形 Checkbox，部分选择通过“已选 n/m”明确显示。
- 超过库存、售罄、商品不存在时拒绝操作；刷新后失效项取消勾选，不能核对结算。库存减少时可通过减号调整至可购数量。
- 底栏角标和首页购物车数量实时同步；合计只统计已选可购项，按整数分计算预估金额。
- 购物车商品信息可打开对应 SKU 的详情；店铺名返回对应店铺；删除最后一件回到空车。
- “核对商品”面板只列所选商品与预估金额，明确收货地址及下单暂未开放，不创建订单。
- 所有写操作互斥，等待期间禁用相关按钮；同次连点不重复加购/删除。修改失败保留原数据。

## 边界与真实状态

- 仍为明确 Mock 模式：无后端请求、账号登录、真实购物车同步或订单创建。
- 购物车保存在当前 App 进程；切页保留，进程结束后清空。正式持久化和会话隔离待用户闭环实现。
- 我的页面仍为旧原型，订单/地址/售后不在本轮实现范围。
- 用户已经认可上一轮逛店风格。本轮首页、详情、规格面板、购物车和空车状态已在 Pura 90 Pro 模拟器实际查看；系统大字体、软键盘与真机仍待后续补测。
- 开发指南步骤 05/07/08/09 仅 Mock 子集推进，不能勾选为真实接口闭环完成。

## 验证与命令

- 闭环 02：assembleHap 成功（16 秒）。
- 闭环 03 最终：assembleHap 成功（17 秒），并已安装到 Pura 90 Pro 模拟器。
- `scripts/check-shopping.cjs` 执行真实模型、Mock Repository、CartViewModel、金额工具源码。通过：商品/SKU 摘要一致、归属校验、售罄、非法数量、相同 SKU 合并、跨店分组、超库存不部分写入、重复提交互斥、单选/店选/全选、数量互不影响、刷新保留选择、批量移除原子性、失效库存恢复、空车计数、搜索与金额精度。
- 自动脚本不模拟 ArkUI 渲染；模拟器已确认首页、详情、SKU 选择与数量、加购、购物车全选/取消、合计、删除确认和空车。软键盘、系统大字体与真机仍需补测。
- 签名配置为空，生成未签名 HAP；不修改个人签名配置。

```powershell
$env:DEVECO_SDK_HOME = 'D:\Coding\IDEs\DevEco Studio\sdk'
& 'D:\Coding\IDEs\DevEco Studio\tools\node\node.exe' 'D:\Coding\IDEs\DevEco Studio\tools\hvigor\bin\hvigorw.js' --mode module -p product=default -p module=entry@default assembleHap --no-daemon
& 'D:\Coding\IDEs\DevEco Studio\tools\node\node.exe' 'scripts/check-shopping.cjs' 'D:\Coding\IDEs\DevEco Studio\sdk\default\openharmony\ets\build-tools\ets-loader\node_modules\typescript'
```

## 用户手动验收（按顺序）

1. 首页搜索“咖啡”应有 2 款；搜索“山野杂货铺”应有 3 款；无结果后点击查看全部恢复 5 款。
2. 进入白茶详情，选择礼盒、数量 2，再加购；加入经典罐装后应分两行，同规格再次加入应合并。售罄规格禁用。
3. 添加咖啡后进入购物车：出现两家店；分别取消单项、整店和全选，检查金额变化和“已选 n/m”。
4. 调整白茶数量，咖啡数量不变；数量 1 时减号禁用，达到库存时加号禁用。
5. 移除商品时选择保留，数据不变；再确认移除，金额和角标同步。批量移除只处理已选商品。
6. 点击购物车的礼盒条目，详情默认选择礼盒 SKU；系统返回回到购物车，不回到首页。
7. 核对商品只包含勾选项，不生成订单；关闭后继续调整。全部取消选择后核对按钮禁用。
8. 删空后显示去逛逛入口、角标消失；重新加购可恢复。切换底栏后购物车数据保留。
9. 检查首页/详情/购物车的小屏、大字体、键盘、底部系统区域和选择面板滚动，反馈实际显示问题。

## 模拟器验收记录

设备：Pura 90 Pro，HarmonyOS 7.0.0 / SDK 26.0.0，2026-09-12。

- 首页、商品详情和空购物车已截图保存到 `docs/验收/2026-09-12/`。
- 白茶选择“双罐礼盒 · 200g”、数量 2 后成功加购：购物车显示 2 件、商品预估合计 ¥476.00、底栏角标为 2。
- 取消商品勾选后，合计变为 ¥0.00，核对按钮禁用；再次勾选后恢复。
- 删除确认弹窗显示正确的商品数；确认移除后购物车显示 0 件、空状态及去逛逛入口，角标消失。
- 验收时发现内容较少时滚动容器默认垂直居中；已为购物车、核对面板和规格面板设置顶部对齐并重新构建验证。

下一轮：先处理本轮运行反馈，再安排登录会话或地址/确认订单闭环；真实后端对接仍需明确环境与测试账号。

## 2026-09-14：闭环 04 网络基础设施

- 新增 `INTERNET` 权限、`NetworkConfig`、`ApiError`、`HttpClient` 与开发期 `SystemApi.ping()`；请求层统一处理 URL 查询参数、JSON、Token 注入、连接/读取超时、HTTP 状态、业务 `code` 和资源释放。
- `NetworkConfig.BASE_URL` 保持为空，避免提交个人局域网地址或误将示例地址当成后端。请求前会明确报出“未配置后端服务地址”。
- SDK 26.0.0 `assembleHap --no-daemon` 构建成功。当前本机 WLAN 地址 `10.251.73.246:8080` 未检测到后端监听，因此 `/api/ping` 的真实设备验收待后端启动且填入实际地址后执行；不能标记为已连通。

## 2026-09-14：闭环 05 消费者登录与会话

- 新增注册、登录、当前用户 API 与消费者用户模型；登录请求只接受消费者角色，注册成功后自动登录。
- “我的”页已替换原有登录占位入口，支持注册/登录、输入校验、提交禁用、错误提示和退出。密码只用于本次请求，不写入本地。
- 会话使用 Preferences 保存 Token 和最小用户信息；启动恢复后调用 `/api/auth/me` 校验，校验失败或退出时清理内存与本地会话。
- SDK 26.0.0 构建成功。因未配置可访问后端，注册、登录、启动恢复及 401 的真实接口联调待网络闭环可用后补验。

## 2026-09-14：闭环 06 真实店铺与商品数据源

- 新增店铺、商品搜索、店铺商品、商品详情和 SKU 可购买状态 API，字段严格对应接口合同；商品图片继续使用本地占位资源，不拼造远程图片地址。
- 浏览 Repository 现在支持显式 `Mock` / `Remote` 两种数据源。默认仍为 Mock，联调时将 `BrowseConfig.source` 改为 `BrowseSource.Remote`；Remote 请求失败会直接进入原有失败重试状态，不会悄悄返回假数据。
- SDK 26.0.0 构建成功。真实列表、中文搜索、详情和 SKU 可购买状态等待后端启动后的设备联调。

## 2026-09-14：闭环 07 真实购物车数据源

- 新增加购、读取、改数量和删除购物车 API，使用后端购物车项 ID 更新/删除，读取后端 `purchasable` 与库存作为当前可结算依据。
- 现有购物车界面与 `CartViewModel` 不重写交互；在 `Remote` 模式下，写操作完成后重新读取权威购物车数据。多选删除按后端现有单项删除接口顺序执行，失败后重新读取，避免客户端继续显示旧状态。
- SDK 26.0.0 构建成功。登录后加购、库存 409、跨店数据、数量更新及删除等待后端启动后的设备联调。

## 2026-09-14：闭环 08 地址管理与原子结算

- 新增地址列表、新增、完整编辑、删除与默认地址的真实接口和页面；字段在提交前按合同长度与必填项校验。
- 购物车“核对商品”现在进入确认订单页，读取真实地址并展示所选商品和预估金额；无地址时明确引导新增地址。
- 下单只调用 `POST /api/checkouts`。同一页面的失败重试复用同一个 `Idempotency-Key`，提交中禁用按钮；成功后显示服务端确认的结算单号、金额和待付款状态，不接入 mock-pay。
- SDK 26.0.0 构建成功。地址 CRUD、跨店原子结算、401、地址失效、库存 409 与超时恢复等待后端启动后的设备联调。

## 参考约定

使用原本地鸿蒙组件资料 `06-控件-输入类.md` 数量范围与数字加减、`07-控件-选择类.md` 勾选及热区、`08-控件-容器类.md` 列表层级与半模态规范。展示组件只通过 Props/回调传递数据；交易状态在 CartViewModel，Mock 写入在 CartRepository，入口导航由 Index 处理。

# 历史快照（以下为各提交时状态，最新以上文为准）

# 当前交接摘要（2026-09-12，闭环 02）

- 当前分支 main；闭环 01 已提交为 `1b71dfa`，用户认可视觉。
- 当前实现：逛店 → 正确商品详情 → 切换 SKU / 数量 → 加入 Mock 购物车 → 查看实际加购内容。此轮即将作为闭环 02 提交。
- 商品页新增主图、价格与库存、店铺入口、规格数量面板、商品说明/信息及固定底部加购区。
- 使用 Navigation / NavDestination 管理详情返回；Index 持有唯一 CartViewModel，在页面之间共享购物车。StoreContent 旧概览已替换为完整详情页。
- CartRepository 按 SKU 合并、校验整数数量与总库存，错误不会部分写入；同一 SKU 保留购物车项 ID，不同规格分开。
- 购物车此时可显示实际商品和小计；勾选、加减和删除在闭环 03 完成。
- 数据明确为 Mock，只保存在当前 App 进程，重启后清空；未接账号或网络，没有新增配送/售后承诺、虚构销量或商品参数。
- 验证：SDK 26.0.0 `assembleHap --no-daemon` 构建成功（16 秒）；`scripts/check-shopping.cjs` 执行实际数据/状态源码，规格归属、摘要一致、售罄、数量校验、合并、跨店、超库存拒绝、重复提交、金额精度通过；git diff --check 通过。
- 状态：实现和自动验证通过，完整设备交互及视觉待验收（未连接设备，未签名 HAP）。
- 手动验收：山野杂货铺 → 云雾白茶 → 选择双罐礼盒/2 件 → 加购 → 查看购物车；回详情改经典罐装再次加购应分两行，重复相同规格应合并。试点售罄规格及反复点击加购。顶部返回/系统返回应回到来源页面。
- 下一小步：购物车单项/店铺/全选、数量修改、确认删除、底栏角标、首页入口与搜索商品。

自动检查命令：

```powershell
& 'D:\Coding\IDEs\DevEco Studio\tools\node\node.exe' 'scripts/check-shopping.cjs' 'D:\Coding\IDEs\DevEco Studio\sdk\default\openharmony\ets\build-tools\ets-loader\node_modules\typescript'
```

# 闭环 01 历史记录（以下保留原始记录，不代表最新状态）

# 当前交接摘要

最后更新日期：2026-09-12
工程路径：`C:\Users\Lenovo\Desktop\ALLCODE\Uni-app`
当前分支与已核验 HEAD：`main` / `1749b65`（本轮改动尚未提交）
接口合同：`docs/harmony-app-api-integration.md`，原内容完整保留；文档基线为 `feature/web-v2` / `f5ca396`，本轮未核验后端运行版本。
当前步骤和小任务：步骤 05 / 07 的 Mock 浏览第一小步——切换店铺 → 对应商品列表；逛店页试验新视觉。
状态：实现已完成，构建与 Mock 数据检查已通过；设备交互及视觉待验收。

## 本次实现

- 三家有稳定 ID 的店铺：山野杂货铺（3 件）、浮光咖啡研究所（2 件，其中 1 件售罄）、慢慢生活馆（空店）。
- StoreSummary / ProductSummary 字段对齐公开接口；Mock 数据由 BrowseRepository 提供，组件不请求网络。
- 切店立即清理上一店商品，通过请求序号防止过期响应覆盖；离开页面忽略异步返回。
- Index 保存选中店铺 ID，切换底栏后返回恢复选择；滚动位置本轮未承诺保持。
- 加载、店铺失败重试、商品失败重试、空目录、空店、售罄状态。
- 卡片打开对应商品概览半模态面板，展示描述、起价、库存；不再跳转固定咖啡原型。完整详情、SKU 与加购尚未实现。
- 移除原先只显示提示文字的搜索框；实际搜索留后续小步。
- 新视觉仅用于逛店页：纸色底、墨色标题、橄榄色细节、横向店铺目录、小圆角商品示意图和细分隔线。首页及全局底栏保留原样。
- 商品示意图为本轮原创 SVG，不是真实商品照片；页面和概览均明确标记。

## 相关文件

- `entry/src/main/ets/model/StoreModels.ets`：接口响应模型。
- `entry/src/main/ets/mock/BrowseMockData.ets`：演示店铺及其商品。
- `entry/src/main/ets/config/BrowseConfig.ets`：明确的 Mock 来源及异常验收场景。
- `entry/src/main/ets/repository/BrowseRepository.ets`：读取店铺与本店商品。
- `entry/src/main/ets/components/store/StoreContent.ets`：页面组合、请求状态、切店及商品概览。
- `entry/src/main/ets/components/product/BrowseProductCard.ets`：纯展示卡片，回调返回商品 ID。
- `entry/src/main/ets/theme/BrowseTheme.ets`：局部试验主题。
- `entry/src/main/ets/pages/Index.ets`：保留选中店铺 ID。
- `entry/src/main/resources/base/media/browse_*.svg`：四张原创商品示意图。

## 验证记录

日期：2026-09-12。

- 使用本机 DevEco Studio SDK 26.0.0 和自带 Node / Hvigor 执行 `assembleHap --no-daemon`，第一轮 BUILD SUCCESSFUL。
- Node 使用 SDK 自带 TypeScript 转译并执行实际 Repository / Mock / Config 源码（临时校验脚本，无额外安装依赖）：店铺商品归属、数量一致性、空店、售罄、未知 ID 拒绝、店铺失败后重试、商品失败后重试、空目录、真实来源不得静默回退 Mock，全部通过。
- `git diff --check` 通过（仅 Windows 行尾转换提示）。
- `hdc list targets` 返回 `[Empty]`，没有已连接设备；未执行实际 ArkUI 视觉和触控验收。仓库签名配置为空，构建产物为未签名 HAP。
- 后续样式微调后的最终构建结果见下方历史记录。

构建命令（PowerShell）：

```powershell
$env:DEVECO_SDK_HOME = 'D:\Coding\IDEs\DevEco Studio\sdk'
& 'D:\Coding\IDEs\DevEco Studio\tools\node\node.exe' 'D:\Coding\IDEs\DevEco Studio\tools\hvigor\bin\hvigorw.js' --mode module -p product=default -p module=entry@default assembleHap --no-daemon
```

## 待用户验收

1. 运行后进入逛店：默认山野杂货铺显示茶、杯、香氛，目录为 3 家。
2. 点击浮光咖啡研究所：只显示两款咖啡，挂耳标记售罄；起价分别为 58.00 和 42.50。
3. 横向滑动店铺目录至慢慢生活馆：显示“本店暂无商品”；点击“逛逛其他店铺”恢复商品。
4. 快速反复切换店铺：标题与最终商品归属一致，不残留上一店商品。
5. 点击不同商品：概览名称、价格、描述、库存与当前商品一致；关闭面板仍停留本店。
6. 从咖啡店切到首页，再回逛店：保持咖啡店选择。
7. 纵向滚动至最后一件商品：底栏不遮挡，最后一行的单件卡片保持半宽。
8. 小屏与系统大字体下检查标题换行、横向店铺目录、卡片文字及概览滚动；目前尚未实测。
9. 开发异常验收：在 BrowseConfig 中把 scenario 分别改为 FailOnce、FailProductsOnce、EmptyStores，重新运行查看失败/重试/空目录，结束后恢复 Normal。此开关不放到消费者页面。

下一小步：先收集本轮视觉和操作反馈，再完成按商品 ID 读取详情、SKU 选择与价格库存联动。不要直接接结算，也不要自动推广新视觉到首页。
用户节奏：逐步教学；2026-09-12 明确授权助手代写当前小步并试验逛店新视觉，完成后讲解与等待运行反馈。

## 参考与素材来源

- 本地组件资料：`D:\ALLAPPS\Note_Apps\Document\HarmonyOS_开发\鸿蒙app\HarmonyOS设计指南`。
- 已参考 `08-控件-容器类.md` 的列表层级、选择反馈与适度装饰，以及半模态面板规范。
- `browse_tea.svg`、`browse_glass.svg`、`browse_scent.svg`、`browse_coffee.svg` 均由本轮直接编写，为项目原创矢量占位资产，不包含外部图片或品牌素材。

# 历史记录

## 2026-09-12

接手时 HEAD 为 1749b65，工作区干净；现有四栏和交易页面为原型，未发现与当前代码对应的设备验收记录。原《开发进度与交接.md》实际装有接口合同，现将原文完整复制到 `docs/harmony-app-api-integration.md`，本文件改为真实进度账本。指南内失效的桌面合同路径同步指向项目副本。

完成 Mock 切店列表及局部视觉试验，尚未提交。上述设备验收结果待用户反馈，不勾选指南步骤 05 / 07 为已完成。

最终验证：完成概览滚动、店铺选中文字颜色、小屏标题及异常场景配置调整后，再次执行上述构建命令，BUILD SUCCESSFUL（14 秒），产物 entry-default-unsigned.hap。设备交互和视觉仍待验收。

用户反馈：认可逛店视觉方向；商品概览信息偏少，要求补齐详情与购买交互。授权将今日工作组织为三个有闭环的本地提交，用户自行推送。先保存本轮 Mock 逛店基线，再推进商品详情/SKU/加购和购物车/首页入口。
